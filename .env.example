# Copy this file to .env and adjust as needed.

# Port the API server listens on.
# Keep this in sync with the client's VITE_API_URL.
PORT=8080

# Secret used to sign JWT auth tokens. Use a long random string in real deployments.
JWT_SECRET=dev-only-change-me-to-a-long-random-string
